export class person {
    constructor(protected name: string, protected gender: string, protected age: number) {
        this.name = name; 
        this.gender = gender; 
        this.age = age;

    };
}